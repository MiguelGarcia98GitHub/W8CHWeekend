sonar.projectKey=MiguelGarcia98GitHub_W8CHWeekend
sonar.organization=miguelgarcia98github

# This is the name and version displayed in the SonarCloud UI.
sonar.projectName=W8CHWeekend
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
sonar.sources=./src

sonar.test.inclusions=./src/**/*.test.*, /src/app.ts, /src/index.ts
/src/router/robots.ts, /src/router/robots.ts

sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.coverage.exclusions=src/index.tsx, src/reportWebVitals.ts, src/**/*.test.*
#sonar.scm.provider=git

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
