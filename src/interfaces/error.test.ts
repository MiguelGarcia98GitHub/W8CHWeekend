// import { HTTPError } from './error';

// describe('Given error.ts', () => {
//     let error: HTTPError;

//     describe('When we instance a new HTTPError', () => {
//         test('Then it should provide a valid error object', () => {
//             error = new HTTPError(418, 'Tea Pot', 'message123');
//             expect(error).toBeInstanceOf(Error);
//             expect(error).toBeInstanceOf(HTTPError);
//             expect(error).toHaveProperty('statusCode', 418);
//             expect(error).toHaveProperty('statusMessage', 'Tea Pot');
//             expect(error).toHaveProperty('message', 'message123');
//             expect(error).toHaveProperty('name', 'HTTPError');
//         });
//     });
// });

export {};
